# Performance

## Targets
- Calendar queries should use indexed user/time-range filters.
- Scheduling algorithms should operate on bounded time windows for interactive use.
- Sync jobs should page provider responses and respect rate limits.
- Frontend calendar views should avoid rendering unnecessary event ranges.

## Benchmarks
Pending implementation of scheduling engine and API endpoints.
